<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Music Library</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>

<body>
    <h1>Music Library </h1>

    <h2>Add a song:</h2>

    <form action="/addMusic" method="POST">
        <input type="text" placeholder="Artist" name="artist" required>
        <input type="text" placeholder="Song" name="song" required>

        <input type="submit">
    </form>

    <table>
        <tr>
            <th>Artist</th>
            <th>Song</th>
            <th></th>
            <th></th>
        </tr>
        <% if(items.length===0) {%>
            <tr>
                <td class="empty">No songs have been added yet</td>
            </tr>
            <% } %>

                <% for(let i=0; i < items.length; i++) {%>
                    <tr class="item">
                        <td class="artist">
                            <%= items[i].artist %>
                        </td>
                        <td class="song">
                            <%= items[i].song %>
                        </td>
                        <td class="fa fa-edit"></td>
                        <td class='fa fa-trash'></td>
                    </tr>
                    <% } %>
    </table>

    <script src='js/main.js'></script>
</body>

</html>